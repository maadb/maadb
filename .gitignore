node_modules/
dist/
.claude/
CLAUDE.md
MAAD.md
.env
.env.*

# Operator/agent overlays — never tracked (Patchnet Master Dev charter)
*.local.md
AGENTS.md

# MAAD engine artifacts — created if the engine is ever pointed at this repo root.
# The source repo is not itself a MAAD project; these should never be tracked.
_backend/
_registry/
_schema/
_skills/
_import/

# OS files
.DS_Store
Thumbs.db
*.swp

# Test temp directories
tests/fixtures/_temp-*/

coverage/
# Internal dev log — local only, public release notes live in GitHub Releases
CHANGELOG.md

# Local scratch (instance configs, throwaway data)
scratch/

# Local profiling / repro harnesses — never tracked (public repo)
scripts/

# Benchmark harness + results — HELD for analysis. First v0.7.16 baseline was
# run on a loaded, unsanitized dev workstation: directional only, not for
# publication. Re-run on a clean dedicated environment before un-ignoring.
bench/
docs/benchmarks.md

# Spec docs — in-flight (docs/specs/) and shipped-for-reference (docs/archive/) — stay local.
# Version.md history and brain decision records carry the durable rationale.
docs/specs/
docs/archive/
